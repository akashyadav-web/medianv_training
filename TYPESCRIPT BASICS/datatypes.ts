// VARIABLES & TYPES

let username: string = "Akash";
let age: number = 25;
let isActive: boolean = true;

// Type inference
let city = "Delhi"; // inferred as string

console.log(username, age, isActive, city);

// Prevents wrong assignment
// age = "twenty";Error

export {};
